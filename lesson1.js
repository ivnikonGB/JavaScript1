// Первый пункт ДЗ
var tC, tF;

tC = parseInt(prompt("Введите температуру в градусах Цельсия:"));
tF = (9 / 5) * tC + 32;
alert("Температура в градусах Фаренгейта: " + tF);

// Второй пункт ДЗ
var admin, name;

name = "Василий";
admin = name;
document.write(admin);

// Третий пункт ДЗ
var a = 1000 + "108";
console.log(a);